import { languages } from "@codemirror/language-data";

/** All fenced-code languages from @codemirror/language-data (markdown, JS, Python, bash, etc.). */
export const fencedCodeLanguages = languages;
